[pytest]
testpaths = tests
asyncio_mode = auto
pythonpath = . tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    conversacion: charla entera contra el modelo real; gasta tokens y necesita API key
